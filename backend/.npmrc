# Make the "engines" field in package.json a hard gate, not a warning.
# Without this, npm only prints EBADENGINE and installs anyway — which is how
# Story 1.4 ended up installing dependencies under Node 20 while building on 24.
# See docs/PROJECT-STANDARDS.md §2 (Node 24.x is pinned by AR-2).
engine-strict=true
