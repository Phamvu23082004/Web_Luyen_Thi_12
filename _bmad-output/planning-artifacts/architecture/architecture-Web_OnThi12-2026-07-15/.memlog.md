---
scope: Whole OnThi12 system — modular-monolith NestJS backend (auth, exam, ai-parsing, submission, dashboard, class) + React/Vite frontend, shared PostgreSQL; all 26 FRs
purpose: build-substrate
altitude: initiative (keeps features)
updated: 2026-07-15T16:43
---

- (direction) Intent=Create (greenfield). Working mode=Coaching. Conversation+spine language=English. Deliverables=spine + evaluation-board doc (HTML) + C4 diagram set + epic-split view.
- (constraint) [ADOPTED from PROJECT-STANDARDS/PRD] Paradigm=modular monolith, one NestJS module per service (auth, exam, ai-parsing, submission, dashboard, class), shared PostgreSQL; modules split into containers later. Reference module=auth.
- (constraint) [ADOPTED AD-01] Every exam originates from an uploaded PDF; no manual authoring path.
- (constraint) [ADOPTED AD-02] AI Parsing is separate + async via RabbitMQ queue; never call Gemini synchronously in an HTTP request.
- (constraint) [ADOPTED AD-03] Lightweight CQRS: Submission owns transactional writes; Dashboard reads pre-aggregated/cached stats, never computes on the write path.
- (constraint) [ADOPTED AD-04] AI never infers correct answers; teacher confirms needs_confirmation before assign.
- (constraint) [ADOPTED] Stack pinned by TechStack.md: React+Vite+TS, TailwindCSS+shadcn/ui, Recharts, TanStack Query; NestJS+TS; PostgreSQL16+Prisma; Redis; RabbitMQ+amqplib; @google/generative-ai (Gemini Flash); Nginx; Docker Compose; GitHub Actions.
- (decision) Table ownership (single writer per table): auth->users; class->classes,class_students,exam_classes; exam->exams,questions; submission->submissions,answer_details; dashboard->class_exam_stats(post-MVP). Binds: all modules. Prevents: two modules writing one table, killing the later container split. Rule: only the owning module may write its tables.
- (decision) Cross-module writes/needs go through the owning module's service interface (in-process now, network after split); no module reaches into another module's tables to write. Prevents: hidden coupling that blocks the monolith->containers split.
- (decision) ai-parsing owns NO tables; the worker produces a parsed-result DTO and calls exam.persistParsedQuestions(examId, result); exam is the ONLY writer of questions. Re-parse idempotency/dedup enforced in that one exam method. Prevents: two writers of questions + duplicate rows on re-parse.
- (decision) Dashboard is the CQRS read-side: read-only queries directly over submissions,answer_details,exams,questions; writes nothing on MVP; all dashboard reads sit behind its own query layer so swapping direct-query -> class_exam_stats/Redis later touches only dashboard. Binds: AD-03, FR-18..FR-25, NFR-02. Prevents: dashboard load leaking onto the submission write path + a breaking change when caching is added.
- (direction) User updated preference mid-run: conversation in Vietnamese (spine/docs stay English). Switch effective now.
- (decision) Exam.status is a state machine Draft->Open->Closed owned by exam module; only the exam's teacher triggers Draft->Open (assign) and Open->Closed (close); no reopen transition. Binds: FR-4,FR-10,FR-12.
- (decision) Assignment gate = single chokepoint in exam.assign(): validates every question (none needs_confirmation; every flagged question has reviewed_at set); only place Draft->Open occurs; service-level invariant, not a DB trigger. Binds: FR-6,FR-7,FR-10. Prevents: an exam going Open with an unconfirmed answer or unreviewed flag.
- (decision) Trust boundary (AD): every controller validates input via DTO + ValidationPipe; role read only from verified JWT; score and is_correct computed server-side only, never accepted from client. Binds: FR-1,FR-2,FR-16, all controllers.
- (decision) Fork1 resolved (A): due_date is an authoritative submission cutoff. Submission service rejects submit when now>due_date OR status!=Open; manual Close = early stop; NO scheduler/auto-close job. due_date comparison must use UTC+7-safe normalization (Date.UTC / ::date). Binds: FR-12,FR-16,NFR. Prevents: needing a cron + missed-flip edge case.
- (decision) Fork2 resolved: add column questions.reviewed_at (timestamptz null) to record explicit teacher flag-acknowledgement (FR-6, missing from SRS section7). null=flag unresolved; set=acknowledged. Assignable when answer_status!=needs_confirmation AND (reviewed_at set OR question was never flagged). This is a data-model addition beyond SRS section7.
- (decision) Blob storage (MVP): local Docker volume mounted into backend, served via static route/Nginx; source PDFs and cropped figure images live there; source_file_url/image_url stay as an abstraction layer so a later swap to S3/MinIO object storage is non-breaking. Binds: FR-4,FR-8,NFR-11. Deferred: object store (MinIO) until scale/HA needs it.
- (decision) Parse-job status tracked via columns on exams (exam-owned): parse_status(pending|parsing|parsed|failed) + parse_error. No parse_jobs table at MVP. Worker updates status ONLY through exam interface (exam.markParsing/persistParsedQuestions/markParseFailed). Bounded retry-count handled in-worker, not persisted. Data-model addition beyond SRS section7.
- (decision) Async parse contract: exam controller receives PDF -> exam creates Draft exam (parse_status=pending) + stores file -> publishes {examId,sourceFileRef} to RabbitMQ -> returns immediately (no Gemini call in request path). ai-parsing worker consumes -> markParsing -> Gemini per page -> DTO -> exam.persistParsedQuestions. Binds: AD-02,FR-5.
- (decision) Re-parse idempotency: re-parse allowed ONLY while Draft; exam.persistParsedQuestions replaces ALL of that exam's questions atomically in one transaction (no duplicate rows on retry). Binds: FR-5,FR-8.
- (constraint) [ADOPTED addendum G] Gemini reliability contract in ai-parsing worker: bounded retry (~3) w/ exp backoff+jitter on transient errors (timeout/5xx/429); non-retryable (quota/malformed/auth) -> no retry + open circuit; on exhaustion/non-retryable -> mark parse_status=failed, PRESERVE source file, clear teacher-facing error + manual 'retry parsing' action; never unbounded retry. Binds: FR-8,NFR-11,SM-5.
- (decision) API contract: success wrapped {data, meta?} via global interceptor; errors use standard NestJS exception filter {statusCode,message,error}; pagination ?page=&limit= with list responses data:[...] + meta:{page,limit,total}; base URL /api; endpoints kebab-case plural. Binds: all controllers. Prevents: divergent response shapes across modules.
- (decision) JWT: minimal claims sub(userId)+role(student|teacher); NO classId in token (class membership looked up from DB). Global JwtAuthGuard + @Public() for login/reset; RolesGuard + @Roles() reads role from verified token only. Binds: FR-1,FR-2, all protected routes.
- (decision) Token strategy (user choice): access + refresh tokens. Short-lived access token verified statelessly per request; long-lived refresh token stored server-side hashed (Redis) with rotation on use; only the /refresh endpoint hits the store. Logout invalidates the stored refresh token. Password reset uses a separate short-lived token.
- (decision) Parse worker (user choice B): separate process from the same backend image (distinct entrypoint, e.g. WORKER=true) consuming RabbitMQ; realizes AD-02 'AI Parsing runs separately', keeps Gemini latency off HTTP threads, scalable independently. Binds: AD-02,FR-5,SM-5.
- (decision) Operational envelope: environments Local (docker compose up) + Production (VPS via GitHub Actions), no staging. Compose services: nginx, backend(HTTP), worker, postgres16, redis, rabbitmq, frontend static served by nginx. Config = 12-factor env (PROJECT-STANDARDS section8); GEMINI_API_KEY/JWT_SECRET/DB creds backend-only, never in frontend bundle, never committed; .env.example committed. Binds: NFR-08,NFR-09,NFR-10.
- (event) All 8 open invariants closed across Rounds A-D; proceeding to distill spine.
- (version) Pinned versions (user-authoritative): Node.js 24.x LTS; NestJS 11.x (requires Node>=20, Express v5 default, SWC compiler); React 19.x; PostgreSQL 18.x. Prisma ORM 7.x (rust-free, per prisma.io changelog Apr 2026 v7.7.0). Supersedes TechStack.md's PostgreSQL 16.
- (version) Remaining stack pinned at current majors: TypeScript 5.x; Vite 7.x; TailwindCSS 4.x; TanStack Query 5.x; Redis 7.x; RabbitMQ 4.x + amqplib; @google/generative-ai (Gemini Flash/Flash-Lite); Nginx; Docker Compose.
- (event) Reviewer Gate: lint 0 findings; 3 parallel reviewers (rubric=adequate, versions=1 critical, adversarial=6 holes/3 critical). Reviews in reviews/.
- (decision) FIX[critical] Gemini SDK @google/generative-ai is EOL (support ended 2025-11-30) -> repin @google/genai. Also Redis 7.x->8.x, Vite 7.x->8.x, TypeScript pin 5.9.x as deliberate hold (TS7 GA 2026-07-08 but tooling not ready).
- (decision) FIX[high] Add AD-19 rate limiting (Redis sliding-window) on login + AI-parse-enqueue endpoints; MVP per SRS 9.6; homes orphaned NFR-09.
- (decision) FIX[critical H1] Add AD-20 exam_attempts entity + deadline snapshot: a started attempt has deadline_at=min(started_at+duration, due_date); auto-submit within deadline_at is ALWAYS accepted regardless of a later Close/due_date change (server-authoritative timer). Fixes lost in-flight attempt vs AD-11. New entity beyond SRS section7.
- (decision) FIX[critical H2+H3] Add AD-21 exam-mutation concurrency + parse fencing: all writes to an exam's questions/status take a row lock on exams; assign() locks + re-validates in-transaction; question writes rejected once status!=Draft; edits locked while parse_status=parsing; persistParsedQuestions applies only if parse_generation matches current (stale worker result discarded).
- (decision) FIX[high H4] Clarify AD-08: the future writer of class_exam_stats is a dashboard-owned aggregation consumer subscribing to a submission.recorded event; submission never writes dashboard tables; read-only CQRS edge preserved.
- (decision) FIX[medium H5] Reword AD-17: access token verified statelessly and valid until short TTL expires; logout revokes the stored REFRESH token only (accepted tradeoff). Removes the stateless-vs-logout contradiction.
- (decision) FIX[medium H6] Extend AD-15 to blobs: single-writer namespaces — api writes exams/<id>/source.pdf; worker writes exams/<id>/gen-<n>/fig-<q>.png (generation-prefixed, tied to parse_generation); write-temp-then-rename for atomic publish.
- (decision) FIX[medium] Pin in conventions: submissions.score scale = 0-10 (one decimal, VN grading); low-confidence flag = ai_confidence < single backend threshold (AI_CONFIDENCE_LOW_THRESHOLD, exam-owned) used by BOTH FR-6 yellow flag and AD-09 gate so they agree.
- (decision) FIX[medium] Add AD-22 delivery & migration safety: GitHub Actions CI (lint/test/build) gates merge; deploy on main; Prisma migrations reversible; DB backup before prod migrate; rollback=redeploy previous image tag; availability focus weekday evenings 19-22h (NFR-05). Homes the silent ops envelope.
- (decision) FIX[low] Make explicit: NFR-03 passwords hashed (bcrypt/argon2) in AD-10; NFR-10 only exam-page imagery (no student PII) sent to Gemini in AD-14.
- (event) Reviewer Gate fixes applied to spine (Stack repin @google/genai/Redis8/Vite8/TS5.9; +AD-19 rate limit, +AD-20 attempt entity, +AD-21 concurrency/parse fence, +AD-22 delivery/ops; AD-08/10/14/15/17 tightened; conventions +score-scale/+confidence-threshold; ERD +exam_attempts/+parse_generation). Lint re-run: 0 findings.
- (event) Reconcile: all 26 FRs homed in Capability map; NFR-01..11 all now covered (NFR-09 via AD-19, NFR-05 via AD-22, NFR-03 in AD-10, NFR-10 in AD-14). 2 deferred OQs (email provider, SM numeric targets) carried to Deferred.
- (event) Notable model additions beyond SRS section7 (surface to user): questions.reviewed_at, questions... exams.parse_status/parse_error/parse_generation, exam_attempts entity, submissions score 0-10 scale. All correctness-necessary.
